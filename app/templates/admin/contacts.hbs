<body class="hold-transition skin-blue sidebar-mini">
    
		{{partial 'nav-bar'}}
      <!-- =============================================== -->

        {{partial 'nav-Menu'}}
        
      <!-- =============================================== -->

      <!-- Content Wrapper. Contains page content -->
      <div class="content-wrapper">

        <!-- Content Header (Page header) -->
        <section class="content-header">
          <h1>
            Tickets
            <small>(Administration Only)</small>
          </h1>
        </section>
        <div id='table'>
        <table class="table table-striped table-bordered">
          <thead>
            <tr>
              <th>ID</th>
              <th>Fullname</th>
              <th>Username</th>
              <th>Email</th>
            </tr>
          </thead>
          <tbody>

            
            <tr>
              <td>{{user.id}}</td>
              <td>{{user.fullname}}</td>
              <td>{{user.username}}</td>
              <td>{{user.email}}</td>
            </tr>
            
          </tbody>
        </table>
        </div>
      </div><!-- /.content-wrapper -->
      
      
</body>
{{outlet}}
